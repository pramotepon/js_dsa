const items = (array1, size) => {
    console.log(array1[0]);
    for (let i = 0; i < size / 2; i++) {
        console.log(array1[i]);
    }
    for (let j = 0; j < 1; j++) {
        console.log(j);
    }
}